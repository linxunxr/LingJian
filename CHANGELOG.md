# 更新日志

本文件记录「灵鉴」各版本的变更内容。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，按版本倒序排列。

发布时，CI 会自动从本文件提取对应版本段落，写入 `latest.json` 的 `notes` 字段，作为客户端更新提示中展示的更新日志。

---

## v0.4.1 - 2026-08-30

### 修复
- **API Key 改为系统凭据管理器加密存储**（安全修复）：此前 API Key 明文存于本机 `settings.json`；现写入 Windows 凭据管理器 / macOS 钥匙串 / Linux Secret Service，配置文件只保留非敏感项。已保存过 Key 的用户升级后首次启动自动迁移（无感），迁移失败时保留明文下次重试，不会丢配置
- **修复系统钥匙串"写入静默失败"**：keyring 依赖缺少平台后端 feature，钥匙串读写实际落在内存假后端（2 个月前因此降级明文存储，本次根治）；开启真实后端后保存/读回经多环节实测
- **MCP 设置开关勾选即保存生效**：此前需点击「应用配置」才持久化，勾选后直接刷新会丢失勾选态，误以为保存无效；保存失败时回读实际状态，不再显示假勾选
- **MCP 启动失败原因可见**：端口被占用等启动失败此前只显示「已停止」无从排查，现设置页直接展示失败原因与处理建议

### 优化
- **设置页布局**：「保存设置」与「测试下载」并排收入 SCF 端点卡片；关于 / 本地数据双栏等高对齐；MCP 卡片开关放大、说明分层（查询类 / 回写类工具分组展示）
- **MCP 写工具描述澄清**：注明评论 / 标签 / 关闭操作经 SCF 服务端代理转发至 GitHub，非客户端直连

---

## v0.4.0 - 2026-08-30

### 新增
- **MCP 二期写能力**（AI 工具接入增强，工具总数达 8 个）：
  - `sync_latest`：从 SCF 拉远端 Issue 列表并增量下载落库，本地已有的自动跳过；服务端日志包已过期的旧上报如实报告不阻断
  - `add_comment` / `update_labels` / `close_issue`：经 SCF 代理回写 GitHub Issue（评论 / 整体替换标签 / 关单），与灵鉴界面操作同一链路
  - **写操作守门**：设置页 MCP 分区新增「允许写操作」开关，默认关闭——关闭时 AI 只能查询不能改动 Issue，开启后才暴露回写能力
- **仓库分支级 CI**：push / PR 到 master 自动跑后端单测 + 前端单测 + 类型检查构建，PR 合并有检查依据

---

## v0.3.0 - 2026-08-30

### 新增
- **MCP 能力开放（AI 工具接入）**：灵鉴内嵌 MCP server，ZCode 等 AI 编码代理接入后可直接查询分析结果，形成"用户上报 → 灵鉴分析 → AI 定位修复"的工作流闭环。默认关闭，设置页「MCP / AI 接入」分区开启（仅监听本机 127.0.0.1，端口可改，开关即改即生效）
  - 开放四个只读工具：`list_issues`（上报列表，含用户反馈原文）、`get_report`（单条上报元信息）、`analyze_report`（错误聚合 / 级别统计 / tag 分布，与灵鉴分析页同源同值）、`query_logs`（按级别 / tag / 关键词过滤分页查日志）
  - 设置页提供 ZCode 配置片段一键复制，接入步骤见 `docs/ZCode MCP 接入设计方案.md`

---

## v0.2.3 - 2026-08-29

### 新增
- **开源协议**：项目以 MIT 协议开源，补 LICENSE 文件与 package.json / Cargo.toml license 字段
- **Gitee 国内镜像**：源码镜像至 [gitee.com/mwcxlinxun/ling-jian](https://gitee.com/mwcxlinxun/ling-jian)；每次发版自动创建 Gitee 发行版（更新日志 + 各平台安装包附件），国内用户可直接下载
- **更新链路三源容灾**：自动更新检查顺序升级为 Gitee → GitHub → 腾讯云 COS，任一源不可用自动切换下一源（本版本发布后下一版本起对存量用户生效）

### 优化
- **安装包文件名统一**：GitHub Release / COS / Gitee 三端产物名统一为 `LingJian_版本_架构` 前缀（如 `LingJian_0.2.3_x64-setup.exe`），修复此前 GitHub 端文件名残缺（缺应用名前缀）的问题

---

## v0.2.2 - 2026-08-29

### 新增
- **分析页展示用户反馈与上报环境**：新增「上报信息」卡片。Issue 分析时展示用户填写的反馈原文，以及版本 / 平台 / 境界 / 游玩时长；从「最近分析」或本地导入进入时展示来源应用等落库信息（用户反馈字段需服务端 SCF 同步部署新版后返回）
- **COS 旧版本自动清理**：每次发布上传后自动清理 COS 上的历史安装包目录，仅保留最近 10 个版本，回滚窗口与存储体积兼顾

### 优化
- **启动失败改为错误弹窗**：数据目录 / 数据库初始化失败时弹出中文错误提示（含具体路径），替代原先的进程白屏崩溃
- **IssueList / AnalyzeView 大文件拆分**：评论 / 标签 / 关闭确认对话框下沉为独立组件，消除两个页面间的重复实现；界面行为不变，后续维护更稳
- **建立前端单元测试体系**：vitest + 组件测试共 51 例，覆盖分析流程状态机、导出、设置、对话框与格式化工具

### 修复
- HTTP User-Agent 版本号不再硬编码旧值，随构建版本自动同步

## v0.2.1 - 2026-08-22

### 优化
- **三页布局去空白**：首页 / 分析页 / 设置页内容列加宽并居中（760→960 / 1080→1400 / 600→860px），大窗口下不再两侧大片留白
- **分析页铺满窗口高度**：日志表格高度从固定 420px 改为随窗口剩余空间自适应，窗口越大可浏览行数越多
- **日志详情空态展示概览**：未选中日志行时，右侧详情栏显示当前匹配条数、日志总量与首末时间，替代单行占位文本
- **设置页卡片双栏排列**：「关于」与「本地数据」卡片并排显示，页面纵向更紧凑（窄窗口自动退回单列）

### 修复
- **未配置 SCF 时问题列表整块空白**：未填写端点配置时，首页问题列表区域什么都不渲染。现在显示引导空态，指引进设置页配置，并说明本地导入日志无需配置

## v0.2.0 - 2026-08-22

### 新增
- **导入鸿蒙应用本地日志**：支持导入 `@huanyinfeng/logger` 库落盘的文本日志（`.log` / `.txt`），可点击「导入本地日志」按钮选择或直接拖入窗口，多选时逐个导入。所有使用该日志库的鸿蒙 App 日志均可分析
- **压缩日志直接导入**：自动按魔数识别并解压 gzip 与 zlib 单流（该库「导出日志」生成的 `.zip` 实为 zlib 单流），无需手动解压
- **FATAL 日志级别**：贯通分析、时间线、错误聚合、过滤与导出，鸿蒙侧 `F` 级崩溃日志不再丢失

### 修复
- **未知级别导致整包解析失败**：上游新增级别（如 TRACE）时降级为 INFO，不再阻断整份日志
- **旧数据库自动迁移**：升级后打开旧版数据库自动补 `app_name` 列，历史记录无损保留

## v0.1.16 - 2026-07-06

### 修复
- **更新内容显示为占位文本**：补全 v0.1.11 ~ v0.1.15 的更新日志段落，CI 提取 CHANGELOG 失败时不再回退到默认值「vX.X.X 更新内容」

## v0.1.15 - 2026-07-06

### 修复
- **关闭 Issue 弹窗不自动关闭**：输入版本号确认关闭后，Issue 已关闭但弹窗卡住不消失。改用 `try/finally` 保证清理，并防御 `labels` 为空时 spread 抛错

## v0.1.14 - 2026-07-06

### 修复
- **首页与关于页版本号滞后**：硬编码的 `v0.1.10` 改为动态读取 `getVersion()`，与更新器共用单一数据源，后续升级自动跟随

## v0.1.13 - 2026-07-05

### 新增
- **关闭 Issue 时记录解决版本号**：关闭弹窗输入挂机仙途版本号后，自动追加版本标签（如 `v0.9.19`）并添加「已在挂机仙途 vX.X.X 中标记为已处理」评论

### 修复
- **安装后仍显示旧版本号**：`tauri.conf.json` 与 `Cargo.toml` 版本号未同步，导致打包内部版本与 `package.json` 不一致

## v0.1.12 - 2026-07-05

### 新增
- **问题列表「已处理」Tab**：在「未处理」与「全部」之间新增按 `closed` 状态筛选的标签页

### 优化
- **COS 上传默认走全球加速域名**：加速域名为主、普通地域域名降为兜底，提升跨洲上传稳定性

## v0.1.11 - 2026-07-05

### 优化
- **SCF 响应解析失败诊断**：SCF 上游响应解析失败时输出原始响应体，便于定位问题

## v0.1.10 - 2026-07-04

### 新增
- **更新说明按 Markdown 渲染**：设置页「检查更新」看到的新版本日志不再显示 `###`、`-`、`**` 等源符号，而是渲染成正常的标题、列表与加粗（采用 marked 解析，适配深色主题）

### 优化
- **COS 上传加速触发阈值 3 → 1**：已购买全球加速流量包，普通域名失败一次即切换 `cos.accelerate.myqcloud.com`，避免在持续慢链路上做无效重试
- 同步刷新全站版本号显示（首页、设置页「关于」此前滞留在旧版本号）

## v0.1.9 - 2026-07-04

### 新增
- **分析详情页支持 Issue 操作**：从首页「最近分析」进入详情页后，可直接关闭/重开 Issue、添加评论、管理标签（此前这些操作只在首页问题列表中）
- 详情页头部显示 Issue 真实状态徽章（未处理/已关闭），关闭/重开按钮文案按远端真实状态切换
- 管理标签对话框回填 Issue 当前真实标签

### 优化
- 首页「最近分析」跳转带上 issue 号，详情页进入时即调用 `fetch_issue_info` 获取真实状态
- `IssueInfo` 结构补 `state`/`labels` 字段（带 serde default，旧版 SCF 不返回时降级为空）

### 修复
- 分析详情页同页换 id 不刷新：补 `route.query.id` 的 watch，并重置 Issue 操作本地状态

## v0.1.8 - 2026-07-04

### 修复
- **最近分析列表显示空记录**：Report 结构体漏加 `#[serde(rename_all = "camelCase")]`，导致 JSON 字段为 snake_case（report_id），前端按 camelCase（reportId）读取全部为 undefined

## v0.1.7 - 2026-07-04

### 优化
- 首页 keep-alive 缓存：从分析页返回首页不再重新加载，保留列表数据与滚动位置
- 最近分析加载增加诊断日志（F12 控制台可见 `[loadRecent]` 输出）

## v0.1.6 - 2026-07-04

### 修复
- 首页白屏：`list_recent_reports` 返回数据字段缺失时 `.slice()` 崩溃，加空值防御
- `list_issues` 返回异常结构时数组兜底，避免 `v-for` 遍历 undefined
- COS 上传网络参数调优，根治跨洲 write timeout
- COS 上传连续失败 3 次后自动切换全球加速域名（失败后自动回退）

### 新增
- 启用 DevTools（F12 调试控制台），便于排查运行时问题

## v0.1.5 - 2026-07-03

### 修复
- COS 关闭全球加速，改回普通地域域名（ap-guangzhou）
- COS 上传增强断点续传：分块重试 + 指数退避 + 碎片清理 + 进度可见
- 首页版本号显示修正

### 新增
- 启用 DevTools（F12 调试控制台），便于排查首页白屏等运行时问题

## v0.1.4 - 2026-07-03

### 修复
- 设置保存后首页仍提示配置不完整：apiKey 改存 settings.json 替代 keyring（部分 Windows 环境 keyring 写入静默失败）
- 应用图标显示为蓝色占位：用 tauri icon 重新生成全套图标，确保与源设计一致

### 优化
- COS 产物按版本分目录归档：安装包上传到 `/<版本>/` 子目录，`latest.json` 留根目录，避免所有版本混在一起
- COS 改用全球加速域名（CI 上传 + 客户端更新 + 用户下载均走加速接入点）

## v0.1.0 - 2026-06-09

首个可用版本：Path of Idle Immortals 日志分析工具。

### 新增
- 日志分析核心：通过 SCF 端点拉取日志、解析分类、本地 SQLite 落库与缓存
- 图表可视化：基于 Chart.js 的时间线图表与错误聚合面板
- 虚拟滚动列表，支持海量日志条目流畅浏览
- 问题列表：自动拉取 Issue、状态切换、点选分析
- Issue 操作：关闭/重开、评论、标签菜单（经 SCF 代理 GitHub）
- Markdown / JSON / CSV 多格式导出
- 凭证加密存储（系统钥匙串 keyring）
- 设置页连接验证与首次启动引导
- 键盘快捷键（Ctrl+F 搜索 / Esc 返回）
- 数据目录便携存储：跟随安装位置、可自定义、自动迁移
- 应用自动更新（Tauri Updater，腾讯云 COS 分发）

### 修复
- 下载日志后解析失败：兼容裸数组与包裹对象两种格式，补全 category 字段

### 优化
- 统一前后端类型契约
- 引导流程简化为两步（SCF URL + API Key）
- 移除 GitHub Token 凭证管理与直连逻辑，统一经 SCF 端点代理
